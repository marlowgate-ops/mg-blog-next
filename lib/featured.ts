/**
 * 人気記事の手動ピック（slugの配列）。
 * ここに slug を入れると、ホーム上部に固定表示されます。
 * 未設定（空配列）の場合は「最新から自動」で代替されます（本番運用が止まらない設計）。
 */
export const FEATURED_SLUGS: string[] = [
  // 'effective-days-checklist',
  // 'winrate-vs-payoff-thinking',
  // 'news-time-filtering',
];
