
name: Ingest broker reviews (docx/pdf/md → markdown)

on:
  workflow_dispatch:
  push:
    paths:
      - 'content_source/reviews/**/*.docx'
      - 'content_source/reviews/**/*.pdf'
      - 'content_source/reviews/**/*.md'

permissions:
  contents: write

jobs:
  convert:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pandoc
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc

      - name: Convert documents to Markdown
        shell: bash
        run: |
          mkdir -p content/reviews
          shopt -s globstar nullglob
          # md -> copy
          for f in content_source/reviews/**/*.md; do
            [ -e "$f" ] || continue
            base=$(basename "$f")
            cp "$f" "content/reviews/$base"
          done
          # docx -> md
          for f in content_source/reviews/**/*.docx; do
            [ -e "$f" ] || continue
            base=$(basename "$f" .docx)
            pandoc "$f" -f docx -t gfm -o "content/reviews/${base}.md"
          done
          # pdf -> md (best-effort)
          for f in content_source/reviews/**/*.pdf; do
            [ -e "$f" ] || continue
            base=$(basename "$f" .pdf)
            pandoc "$f" -f pdf -t gfm -o "content/reviews/${base}.md" || true
          done

      - name: Commit & push
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore(content): ingest reviews (→ md)"
          file_pattern: content/reviews/*.md
